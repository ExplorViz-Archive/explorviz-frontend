<div class="mb-3">
  <HelpTooltip @title={{@setting.description}} />
  <label for={{@setting.name}}>{{@setting.displayName}}</label>
  <input
    id={{@setting.name}}
    value={{@setting.value}}
    type="number"
    step="0.1"
    class="form-control"
    {{on "change" @onChange}}
  >
</div>