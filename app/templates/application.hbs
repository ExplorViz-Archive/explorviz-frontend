<div class="d-flex flex-row vh-100">
  {{#if session.isAuthenticated}}
    {{#let (component "page-setup/navbar") as |Navbar|}}
      <Navbar />
    {{/let}}
  {{/if}}

  <div id="vizContainer" class="main-content">
    {{outlet}}
  </div>
</div>