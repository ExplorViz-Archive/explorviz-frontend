<UserManagement::UserList
  @refreshUsers={{route-action "refreshRoute"}}
  @users={{@model}}
  @page={{this.page}}
  @size={{this.size}}
  @changePageSize={{this.changePageSize}}
  @goToPage={{this.goToPage}}
  @deleteUsers={{this.deleteUsers}}
  @pageSizes={{this.pageSizes}}
/>