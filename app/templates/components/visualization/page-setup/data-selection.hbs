<div class="">
  <button type="button" class="btn btn-outline-dark" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div>
{{#ember-scrollable}}
  <div class="pr-3 ml-2">
    {{#each content as |componentName|}}
      <div class="card mb-3">
        <div class="card-body">
          {{componentName}}
        </div>
      </div>
    {{/each}}
    {{#each content as |componentName|}}
      <div class="card mb-3">
        <div class="card-body">
          {{componentName}}
        </div>
      </div>
    {{/each}}
    {{#each content as |componentName|}}
      <div class="card mb-3">
        <div class="card-body">
          {{componentName}}
        </div>
      </div>
    {{/each}}
    {{#each content as |componentName|}}
      <div class="card mb-3">
        <div class="card-body">
          {{componentName}}
        </div>
      </div>
    {{/each}}
    {{#each content as |componentName|}}
      <div class="card mb-3">
        <div class="card-body">
          {{componentName}}
        </div>
      </div>
    {{/each}}
    {{#each content as |componentName|}}
      <div class="card mb-3">
        <div class="card-body">
          {{componentName}}
        </div>
      </div>
    {{/each}}
    {{#each content as |componentName|}}
      <div class="card">
        <div class="card-body">
          {{componentName}}
        </div>
      </div>
    {{/each}}
  </div>
{{/ember-scrollable}}