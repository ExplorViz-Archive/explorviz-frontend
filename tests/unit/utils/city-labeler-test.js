import cityLabeler from 'explorviz-ui-frontend/utils/city-labeler';
import { module, test } from 'qunit';

module('Unit | Utility | city labeler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cityLabeler();
  assert.ok(result);
});
