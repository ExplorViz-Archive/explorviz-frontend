import TeleportMesh from 'explorviz-frontend/utils/teleport-mesh';
import { module, test } from 'qunit';

module('Unit | Utility | teleport-mesh', function(/*hooks*/) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = new TeleportMesh();
    assert.ok(result);
  });
});
