import landscapeRenderingModelReducer from 'explorviz-frontend/utils/landscape-rendering/model-reducer';
import { module, test } from 'qunit';

module('Unit | Utility | landscape-rendering/model-reducer', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = landscapeRenderingModelReducer();
    assert.ok(result);
  });
});
