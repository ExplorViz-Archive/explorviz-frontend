//import landscapeRenderingNavigation from 'explorviz-ui-frontend/utils/landscape-rendering/navigation';
import { module, test } from 'qunit';

module('Unit | Utility | landscape rendering/navigation');

// Replace this with your real tests.
test('it works', function(assert) {
  //let result = landscapeRenderingNavigation();
  //assert.ok(result);
  assert.equal(true, true);
});
