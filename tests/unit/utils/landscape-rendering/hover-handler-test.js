//import landscapeRenderingHoverHandler from 'explorviz-ui-frontend/utils/landscape-rendering/hover-handler';
import { module, test } from 'qunit';

module('Unit | Utility | landscape rendering/hover handler');

// Replace this with your real tests.
test('it works', function(assert) {
  //let result = landscapeRenderingHoverHandler();
  //assert.ok(result);
  assert.equal(true, true);
});
