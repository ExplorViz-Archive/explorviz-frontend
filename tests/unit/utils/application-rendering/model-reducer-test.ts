/* import ModelReducer from 'explorviz-frontend/utils/application-rendering/model-reducer'; */
import { module, test } from 'qunit';

module('Unit | Utility | application-rendering/model-reducer', function(/* hooks */) {

  // Replace this with your real tests.
  test('it works', function(assert) {
/*     let result = ModelReducer(); */
    assert.ok(true);
  });
});
