/* import applicationRenderingCommunicationRendering
  from 'explorviz-frontend/utils/application-rendering/communication-rendering'; */
import { module, test } from 'qunit';

module('Unit | Utility | application rendering/communication rendering', function (/* hooks */) {
  // Replace this with your real tests.
  test('it works', (assert) => {
    // let result = applicationRenderingCommunicationRendering();
    assert.equal(true, true);
  });
});
