//import applicationRenderingNavigation from 'explorviz-ui-frontend/utils/application-rendering/navigation';
import { module, test } from 'qunit';

module('Unit | Utility | application rendering/navigation');

// Replace this with your real tests.
test('it works', function(assert) {
  //let result = applicationRenderingNavigation();
  //assert.ok(result);
  assert.equal(true, true);
});
