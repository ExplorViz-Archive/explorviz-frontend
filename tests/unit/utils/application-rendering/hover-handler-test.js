//import applicationRenderingHoverHandler from 'explorviz-ui-frontend/utils/application-rendering/hover-handler';
import { module, test } from 'qunit';

module('Unit | Utility | application rendering/hover handler');

// Replace this with your real tests.
test('it works', function(assert) {
  //let result = applicationRenderingHoverHandler();
  //assert.ok(result);
  assert.equal(true, true);
});
