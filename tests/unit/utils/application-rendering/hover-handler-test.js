//import applicationRenderingHoverHandler from 'explorviz-frontend/utils/application-rendering/hover-handler';
import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Utility | application-rendering/hover-handler', function(hooks) {

  setupTest(hooks);

  // Replace this with your real tests.
  test('it works', function(assert) {
    //let result = applicationRenderingHoverHandler();
    assert.ok(true);
  });
});
