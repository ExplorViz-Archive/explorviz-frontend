/* import applicationRenderingEntityRendering
  from 'explorviz-frontend/utils/application-rendering/entity-rendering'; */
import { module, test } from 'qunit';

module('Unit | Utility | application rendering/entity rendering', function (/* hooks */) {
  // Replace this with your real tests.
  test('it works', (assert) => {
    // let result = applicationRenderingEntityRendering();
    assert.equal(true, true);
  });
});
