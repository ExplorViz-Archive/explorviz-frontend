/* import applicationRenderingHighlighting
  from 'explorviz-frontend/utils/application-rendering/highlighting'; */
import { module, test } from 'qunit';

module('Unit | Utility | application rendering/highlighting', function (/* hooks */) {
  // Replace this with your real tests.
  test('it works', (assert) => {
    // let result = applicationRenderingHighlighting();
    assert.equal(true, true);
  });
});
