import UserListMenu from 'explorviz-frontend/utils/menus/user-list-menu';
import { module, test } from 'qunit';

module('Unit | Utility | user-list-menu', function(/* hooks */) {

  test('it exists', function(assert) {
    // let result = UserListMenu();
    assert.ok(true);
  });
});
