import packageHelpers from 'explorviz-frontend/utils/package-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | package-helpers', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = packageHelpers();
    assert.ok(result);
  });
});
