import landscapeSchemesStructureData from 'explorviz-frontend/utils/landscape-schemes/structure-data';
import { module, test } from 'qunit';

module('Unit | Utility | landscape-schemes/structure-data', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = landscapeSchemesStructureData();
    assert.ok(result);
  });
});
