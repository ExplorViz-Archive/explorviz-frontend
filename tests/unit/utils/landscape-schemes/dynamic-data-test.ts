import landscapeSchemesDynamicData from 'explorviz-frontend/utils/landscape-schemes/dynamic-data';
import { module, test } from 'qunit';

module('Unit | Utility | landscape-schemes/dynamic-data', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = landscapeSchemesDynamicData();
    assert.ok(result);
  });
});
