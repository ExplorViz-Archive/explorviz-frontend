import { module /* test */ } from 'qunit';

module('Unit | Utility | ar-zoom-helper', function () {

  // Replace this with your real tests.
/*   test('it works', function(assert) {
    let result = landscapeStructureHelpers();
    assert.ok(result);
  }); */
});
