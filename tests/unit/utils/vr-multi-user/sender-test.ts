import { module, test } from 'qunit';
// import VrMessageSender from 'virtual-reality/utils/vr-multi-user/sender';

module('Unit | Utility | vr-multi-user/sender', function(/* hooks */) {

  test('it exists', function(assert) {
    // let service = this.owner.lookup('service:web-socket');
    // let sender = new VrMessageSender(service);
    assert.ok(true);
  });
});
