import objectHelpers from 'explorviz-frontend/utils/object-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | object-helpers', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = objectHelpers();
    assert.ok(result);
  });
});
