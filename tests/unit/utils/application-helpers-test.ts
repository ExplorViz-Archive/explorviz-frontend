import applicationHelpers from 'explorviz-frontend/utils/application-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | application-helpers', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = applicationHelpers();
    assert.ok(result);
  });
});
