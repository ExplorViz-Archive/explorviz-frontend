import collaborativeInteraction from 'explorviz-frontend/utils/collaborative-interaction';
import { module, test } from 'qunit';

module('Unit | Utility | collaborative-interaction', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = collaborativeInteraction();
    assert.ok(result);
  });
});
