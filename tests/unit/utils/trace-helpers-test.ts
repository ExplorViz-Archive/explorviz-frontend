import traceHelpers from 'explorviz-frontend/utils/trace-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | trace-helpers', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = traceHelpers();
    assert.ok(result);
  });
});
