/* import applicationRenderingInteraction
  from 'explorviz-frontend/utils/application-rendering/interaction'; */
import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Utility | application rendering/interaction', function (hooks) {
  setupTest(hooks);

  // Replace this with your real tests.
  test('it works', function (assert) {
    // let result = applicationRenderingInteraction();
    // assert.ok(result);
    assert.equal(true, true);
  });
});
