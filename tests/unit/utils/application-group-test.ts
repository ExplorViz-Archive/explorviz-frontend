import applicationGroup from 'explorviz-frontend/utils/application-group';
import { module, test } from 'qunit';

module('Unit | Utility | application-group', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = applicationGroup();
    assert.ok(result);
  });
});
