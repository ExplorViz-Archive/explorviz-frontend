import mousePingHelper from 'explorviz-frontend/utils/mouse-ping-helper';
import { module, test } from 'qunit';

module('Unit | Utility | mouse-ping-helper', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = mousePingHelper();
    assert.ok(result);
  });
});
