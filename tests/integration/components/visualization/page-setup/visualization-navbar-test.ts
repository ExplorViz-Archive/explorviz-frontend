import { moduleForComponent, test } from 'ember-qunit';
//import hbs from 'htmlbars-inline-precompile';

moduleForComponent('visualization/page-setup/visualization-navbar', 'Integration | Component | visualization/page setup/visualization navbar', {
  integration: true
});

test('it renders', function(assert) {

  // Set any properties with this.set('myProperty', 'value');
  // Handle any actions with this.on('myAction', function(val) { ... });

  //this.render(hbs`{{visualization/page-setup/visualization-navbar}}`);

  //assert.equal(this.$().text().trim(), '');

  // Template block usage:
  //this.render(hbs`
  //  {{#visualization/page-setup/visualization-navbar}}
  //    template block text
  //  {{/visualization/page-setup/visualization-navbar}}
 // `);

  assert.equal(true, true);
});
