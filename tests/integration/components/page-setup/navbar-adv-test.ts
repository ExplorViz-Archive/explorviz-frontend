/*import { module, test } from 'qunit';
import { setupRenderingTest } from 'ember-qunit';
import { render } from '@ember/test-helpers';
import hbs from 'htmlbars-inline-precompile';

module('Integration | Component | page-setup/navbar-adv', function(hooks) {
  setupRenderingTest(hooks);

  // https://github.com/DockYard/ember-route-action-helper/issues/42

  test('it renders', async function(assert) {
    // Set any properties with this.set('myProperty', 'value');
    // Handle any actions with this.set('myAction', function(val) { ... });
    await render(hbs`{{page-setup/navbar-adv}}`);

    assert.ok(this.element.textContent.trim().includes('Configuration'));
  });
});
*/