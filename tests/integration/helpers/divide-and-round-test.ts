import { module, test } from 'qunit';
import { setupRenderingTest } from 'ember-qunit';
// import { render } from '@ember/test-helpers';
// import hbs from 'htmlbars-inline-precompile';

module('Integration | Helper | divide-and-round', function (hooks) {
  setupRenderingTest(hooks);

  // Replace this with your real tests.
  test('dummy', async function (assert) {
    /*
    const value = 42;
    const divider = 2;
    const decimals = 1;

    await render(hbs`{{divide-and-round this.value this.divider this.decimals}}`);

    assert.equal(this.element.textContent!.trim(), '21.0');
    */

    assert.equal(true, true);
  });
});
